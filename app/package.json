{
  "name": "iperc-pymes",
  "version": "1.1.0",
  "main": "server.js",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.948.0",
    "@aws-sdk/lib-dynamodb": "^3.948.0",
    "@aws-sdk/util-dynamodb": "^3.948.0",
    "blob-stream": "^0.1.3",
    "express": "^4.18.2",
    "pdfkit": "^0.17.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "quality": "npm run lint && npm test && npm audit"
  }
}
